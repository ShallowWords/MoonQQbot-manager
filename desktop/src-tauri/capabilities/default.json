{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认权限集。本壳只负责显示面板，不需要任何系统能力——前端通过 HTTP 直接调后端服务，不走 Tauri IPC。因此只保留 core:default。",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}